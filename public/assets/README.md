# Legend of Darkness - 게임 에셋 가이드

> ⚠️ **AI 에이전트 필독**: 이 문서를 에셋 생성/수정 전에 반드시 참조하세요.
> 
> 📋 **참고 문서**: `/DESIGN_GUIDELINES.md` (디자인 상세 가이드)

---

아이소매트릭 쿼터뷰 (어둠의전설)

TODO: 스프라이트시트 생성

현재 스프라이트시트 파일들은 레거시 단일 이미지를 이름만 변경한 것입니다. 실제 4행 x 6열 스프라이트시트로 교체해야 합니다.

이미지 생성 API 할당량이 리셋되면 (약 2시간 후) 새 스프라이트시트를 생성할 수 있습니다.

대신 건물은 여러 타일일 수 있습니다. 건물이나 지형지물만 여러 타일일 수 있어요 몬스터 사용자만 1타일입니다 크기와 상관 없이

건물도 여러 디자인이 필요합니다.


## 🎮 게임 스타일 핵심 요약

| 항목 | 설정 |
|------|------|
| **시대** | 90년대 RPG (Legend of Darkness 스타일) |
| **장르** | 다크 판타지 (Dark Fantasy) |
| **시점** | 아이소메트릭 2.5D (대각선 30도) | 아이소매트릭 쿼터뷰 (어둠의전설)
| **렌더링** | **스프라이트시트 기반** |
| **분위기** | 어둡고, 우울하고, 위협적인 |

### 금지 사항 ❌
- 귀여운/치비 스타일
- 밝은 파스텔 색상
- 카툰/만화 스타일
- 친근한 동물 (쥐, 토끼 등은 흉측하게)

---

## 📐 스프라이트 시스템 규격

### 핵심 규칙: 1타일 = 1엔티티

> **중요**: 모든 캐릭터와 몬스터는 **시각적 크기와 관계없이 1타일(1칸)만 차지**합니다.
> 
> - 드래곤이 128x128px 크기여도 **논리적으로 1칸**
> - 슬라임이 32x32px 크기여도 **논리적으로 1칸**
> - 충돌 판정, 이동, 타겟팅 모두 타일 중심점 기준

### 스프라이트시트 레이아웃 (필수)

```
┌─────────────────────────────────────────────────┐
│ Frame 0 │ Frame 1 │ Frame 2 │ Frame 3 │ ...    │
├─────────────────────────────────────────────────┤
│ Row 0: IDLE   (대기)     - 4 프레임             │
│ Row 1: MOVE   (이동)     - 4 프레임             │
│ Row 2: ATTACK (공격)     - 6 프레임             │
│ Row 3: DEATH  (사망)     - 4 프레임             │
└─────────────────────────────────────────────────┘
```

### 프레임 크기

| 엔티티 타입 | 프레임 크기 | 비고 |
|-------------|-------------|------|
| 캐릭터 | 64x64 px | 모든 클래스 동일 |
| 소형 몬스터 (Lv 1-10) | 32x32 px | 슬라임, 쥐, 박쥐 |
| 중형 몬스터 (Lv 11-50) | 48x48 px | 스켈레톤, 고블린 |
| 대형 몬스터 (Lv 51+) | 64x64 px | 보스급 |
| 보스 | 128x128 px | 드래곤 등 |
| 타일 (바닥) | 64x48 px | 아이소메트릭 다이아몬드 |
| 스킬 이펙트 | 64x64 px | 최소 6프레임 |

---

## 📁 폴더 구조 (새 구조 - 스프라이트 기반)

```
assets/
├── characters/                    # 플레이어 캐릭터
│   ├── warrior/
│   │   ├── male_spritesheet.png   # 남성 전사 (4행 x N열)
│   │   └── female_spritesheet.png # 여성 전사
│   ├── mage/
│   ├── rogue/
│   ├── cleric/
│   └── martial_artist/
│
├── monsters/                      # 몬스터
│   ├── slime/
│   │   └── spritesheet.png        # 슬라임 스프라이트시트
│   ├── rat/
│   ├── bat/
│   ├── wolf/
│   ├── skeleton/
│   ├── goblin/
│   ├── ghost/
│   └── dragon/
│
├── skills/                        # 스킬 이펙트
│   ├── warrior/
│   │   └── slash_fx.png           # 베기 이펙트 (6프레임)
│   ├── mage/
│   │   ├── fireball_fx.png
│   │   ├── icebolt_fx.png
│   │   └── lightning_fx.png
│   ├── rogue/
│   ├── cleric/
│   └── common/
│       ├── hit_impact.png         # 타격 이펙트
│       └── critical_fx.png        # 크리티컬 이펙트
│
├── tiles/
│   ├── ground/                    # 바닥 타일
│   │   └── tileset.png            # 통합 타일셋
│   ├── buildings/                 # 건물
│   │   └── buildings.png          # 통합 건물 시트
│   └── decorations/               # 장식물
│       ├── torch.png              # 횃불 (애니메이션)
│       └── props.png              # 기타 소품
│
├── audio/
│   ├── bgm/                       # 배경음악
│   │   ├── village.mp3
│   │   ├── dungeon.mp3
│   │   └── boss.mp3
│   └── sfx/                       # 효과음
│       ├── attack/
│       ├── skill/
│       ├── ui/
│       └── ambient/
│
├── ui/                            # UI 요소
│   ├── inventory_bg.png
│   ├── buttons.png
│   └── icons/
│
└── maps/                          # 맵 데이터
    └── milles.json
```

---

## 🎨 색상 팔레트

### 다크 판타지 필수 색상

```
배경/어둠:     #0a0a0a, #1a1a2e, #16213e
피/경고:       #8b0000, #660000, #cc0000
마법/신비:     #4a0080, #2d1b4e, #1a0a2e
신성/희망:     #8b7355, #c5c6c7 (희미하게)
독/저주:       #2d4a22, #3d1a4f
금속/강철:     #4a5568, #2d3748
```

### 금지 색상
- 순수 흰색 (#ffffff) - 희미한 회백색 사용
- 파스텔 핑크, 하늘색, 민트색
- 채도 높은 밝은 색상

---

## 🔧 코드 통합 가이드

### 스프라이트 로딩 (Rust/WASM)

```rust
use crate::client::game::systems::{SpriteLoader, AnimationState};

// 캐릭터 스프라이트 경로
let path = SpriteLoader::get_character_sprite_path("warrior", "male", AnimationState::Idle);
// 결과: "/assets/characters/warrior/male_spritesheet.png"

// 몬스터 스프라이트 경로
let path = SpriteLoader::get_monster_sprite_path("slime", AnimationState::Attack);
// 결과: "/assets/monsters/slime/spritesheet.png"
```

### 애니메이션 프레임 계산

```rust
use crate::client::game::systems::AnimationCalculator;

// 일반 루프 애니메이션 (idle, move)
let frame = AnimationCalculator::get_frame_index(
    current_time_ms,
    frame_count,      // 4
    frame_duration_ms // 150
);

// 공격 애니메이션 (시작 시간 기준)
let frame = AnimationCalculator::get_attack_frame_index(
    current_time_ms,
    attack_start_time,
    frame_count,      // 6
    attack_duration   // 500ms
);
```

### 오디오 재생

```rust
use crate::client::game::systems::{AudioManager, audio_paths};

let mut audio = AudioManager::new();

// BGM 재생
audio.play_bgm(audio_paths::BGM_VILLAGE);

// 효과음
audio.play_sfx(audio_paths::SFX_SWORD_SWING);
```

---

## ⚡ 애니메이션 타이밍

| 애니메이션 | 프레임 수 | 총 시간 | 프레임당 시간 |
|-----------|----------|---------|--------------|
| Idle | 4 | 600ms | 150ms |
| Move | 4 | 400ms | 100ms |
| Attack | 6 | 500ms | ~83ms |
| Death | 4 | 600ms | 150ms |
| Skill FX | 6+ | 800ms | ~133ms |

---

## 📌 파일 네이밍 규칙

### 캐릭터
```
/assets/characters/{class}/{gender}_spritesheet.png

예시:
/assets/characters/warrior/male_spritesheet.png
/assets/characters/mage/female_spritesheet.png
```

### 몬스터
```
/assets/monsters/{monster_type}/spritesheet.png

예시:
/assets/monsters/slime/spritesheet.png
/assets/monsters/dragon/spritesheet.png
```

### 스킬 이펙트
```
/assets/skills/{class}/{skill_name}_fx.png

예시:
/assets/skills/mage/fireball_fx.png
/assets/skills/common/hit_impact.png
```

---

## ✅ AI 에셋 생성 체크리스트

에셋 생성 전 확인:

- [ ] 아이소메트릭 2.5D 시점인가?
- [ ] 다크 판타지 분위기인가? (어둡고 우울한 색상)
- [ ] 90년대 RPG 픽셀아트 느낌인가?
- [ ] 스프라이트시트 형식인가? (행: 상태, 열: 프레임)
- [ ] 올바른 프레임 크기인가?
- [ ] 투명 배경 PNG인가?
- [ ] 귀여운/밝은 요소가 없는가?
- [ ] 파일명이 규칙을 따르는가?

---

## 🎵 오디오 가이드

### BGM 스타일
- 메인 테마: 장엄하고 어두운 오케스트라
- 마을: 평화롭지만 우울한 멜로디
- 던전: 긴장감, 에코, 저음 드론
- 보스: 격렬하고 웅장한

### SFX 스타일
- 타격: 묵직하고 임팩트 있게
- 마법: 신비롭고 강렬하게
- UI: 미묘하고 마법적으로

---

_마지막 업데이트: 2024-12-22_
_버전: 2.0 (스프라이트 시스템 통합)_
